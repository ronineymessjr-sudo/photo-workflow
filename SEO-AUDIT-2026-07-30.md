# PhotoAtelier SEO 审计报告

审计日期：2026-07-30  
生产站点：https://photoatelier.pages.dev/  
工作台：https://photoatelier.pages.dev/legacy/?mode=public-beta

## 1. 执行结论

当前状态：**SEO 基础设施已经具备，但尚未进入有效获客阶段。**

| 维度 | 评级 | 判断 |
| --- | --- | --- |
| 抓取与收录 | D | 可抓取，但公开搜索未发现有效收录 |
| 首页元数据 | B | title、description、canonical、OG、hreflang 基本完整 |
| 内容竞争力 | D | 只有 4 个介绍页，没有可承接具体搜索意图的专题页 |
| 多语言 | C- | URL 和 hreflang 正确，但英文、日文、韩文页仍保留大量中文主体内容 |
| 技术交付 | C | 页面体积可控，但正式发布包漏掉一个首屏动效模块 |
| 结构化数据 | C- | JSON-LD 可解析，但 SoftwareApplication 类型和必填信息不完整 |
| 数据监测 | F | 未发现 Search Console、Bing 或流量分析接入标记 |

不建议当前直接以 Reddit、Product Hunt 或大规模内容投放作为主动作。应先修复 P0 技术问题，建立 5–8 个有实际价值的摄影专题页，并接入可验证的搜索数据。

## 2. 已经做对的部分

- `/`、`/en/`、`/ja/`、`/ko/` 均返回 HTTP 200。
- `robots.txt` 允许主流搜索和 AI 抓取，并声明了 sitemap。
- `sitemap.xml` 可访问，包含四个语言入口。
- 四个介绍页均有独立 title、description、自引用 canonical。
- 四个介绍页均配置 5 组 hreflang，包括 `x-default`。
- 首页只有一个 H1。
- 首页 9 张图片全部有 alt、width、height；首图设置 `fetchpriority="high"`，其余图片延迟加载。
- 首页存在可解析的 `SoftwareApplication` JSON-LD。
- 首屏主图只有约 64 KiB，CSS 约 20 KiB，图片本身并不是当前最大问题。

## 3. 关键问题

### P0-1：介绍页首屏模块发布缺失

首页引用：

`/src/landing-motion.js`

源文件存在，约 12.7 KiB，但没有进入 `dist-v2`。线上请求返回的是 HTML 回退页，Content-Type 为 `text/html`，浏览器会拒绝将其作为 JavaScript 模块执行。

影响：

- 时间线和滚动动效可能不执行。
- 浏览器控制台产生 MIME 错误。
- 影响 Best Practices、交互体验和后续真实用户指标。

修复：

- 将 `src/landing-motion.js` 加入 `tools/build-v2-dist.js` 的发布复制清单。
- 增加 dist 测试：文件存在、Content-Type 为 JavaScript、响应正文不是 HTML。

### P0-2：工作台 canonical 和社交元数据仍指向旧站

`/legacy/` 当前仍声明：

- canonical：`https://ronineymessjr-sudo.github.io/photo-workflow/`
- og:url：旧 GitHub Pages
- og:image：旧 GitHub Pages 图片

这会要求搜索引擎把当前 Cloudflare 页面产生的信号归到旧域名。canonical 是强信号，不能保留错误目标。

建议：

- 工作台是登录型产品页面，建议添加 `noindex,follow`。
- canonical 改为 `https://photoatelier.pages.dev/legacy/`，不要包含 `mode`、`refresh` 等参数。
- OG/Twitter 图片全部切换到当前正式域名。
- 搜索排名由公开介绍页和专题内容页承担，不让 830 KiB、8 个 H1 的应用壳参与排名。

### P0-3：当前没有可确认的搜索监测

代码中未发现：

- Google Search Console 验证标记
- Bing Webmaster 验证标记
- GA4、Plausible、Umami、Clarity 或 Cloudflare Web Analytics 脚本

因此目前无法回答曝光、查询词、点击、索引覆盖率和 Core Web Vitals 的真实趋势。

建议：

1. 绑定自有域名后建立 Google Search Console Domain Property。
2. 提交 `sitemap.xml`，使用 URL Inspection 请求首页和首批专题页收录。
3. 同步接入 Bing Webmaster Tools。
4. 产品早期优先使用 Cloudflare Web Analytics 或轻量隐私分析；需要广告归因时再加 GA4。
5. 记录 CTA 点击、进入登录页、点击体验、注册完成、生成首个方案五个转化事件。

## 4. 影响排名的内容问题

### P1-1：搜索引擎可排名页面只有 4 个

站点地图目前只有四个语言首页，没有摄影方法、模板、案例或工具说明页。首页中文可见文本规模约 198 个空白分隔单元，主要用于产品展示，无法覆盖具体搜索需求。

建议首批建立以下 6 个中文专题：

| URL 建议 | 主搜索意图 | 必须提供的真实价值 |
| --- | --- | --- |
| `/guides/reference-image-to-shot-plan/` | 参考图怎么变成拍摄方案 | 完整示例、拆镜头方法、可下载模板 |
| `/guides/portrait-shot-list/` | 人像摄影分镜/镜头清单 | 景别、焦段、动作口令、耗时 |
| `/guides/posing-prompts/` | 摄影师引导动作话术 | 可执行口令、常见失败方式 |
| `/guides/photo-shoot-schedule/` | 拍摄日程安排模板 | 准备、拍摄、选片、交付流程 |
| `/guides/lut-workflow/` | LUT 怎么用 | Resolve、Photoshop、手机流程与限制 |
| `/guides/log-to-rec709/` | S-Log3、D-Log M、Apple Log 转换 | 输入色彩空间、技术 LUT、创意 LUT 区别 |

每页应包含：

- 原创摄影案例或产品内真实操作截图。
- 一份可下载或可复制的清单。
- 明确作者/审核者、更新时间和资料来源。
- 相关专题与产品功能的内部链接。
- `Article`、`BreadcrumbList`；只有页面真实显示问答时才使用 FAQ 数据。

Google 明确建议优先创建有原创经验、完整解释和真实帮助价值的内容，而不是批量生成关键词页面。

### P1-2：多语言页没有完成主体翻译

英文、日文、韩文页面虽然具有正确的 lang、title、description、canonical 和 hreflang，但前六个主要 H2/H3 仍是中文。Google 会根据可见内容判断语言；只翻译标题、页脚或少量段落可能被视为未充分本地化。

建议：

- 在发布前建立“非目标语言字符比例”测试。
- 英文优先完整翻译，因为它承担 Reddit、Product Hunt 和海外搜索入口。
- 日文、韩文在无人校对前可暂时不进入 sitemap，避免低质量语言页被率先抓取。
- 每个语言版本必须互相声明完整 hreflang，并保留同语言 canonical。

### P1-3：结构化数据暂不满足软件富结果要求

首页 JSON-LD 使用 `applicationCategory: "PhotographyApplication"`。Google 文档列出的支持类型中没有该值；可考虑 `DesignApplication`、`MultimediaApplication` 或更符合实际功能的支持类型。

首页还缺少软件富结果所需的完整 Offer，以及真实 rating/review 信号。

建议：

- 添加 `offers: {"@type":"Offer","price":"0","priceCurrency":"CNY"}`。
- 不要编造评分。只有真实收集并在页面展示评价后，才添加 AggregateRating 或 Review。
- 使用 Rich Results Test 和 Search Console 验证，而不是只检查 JSON 能否解析。

## 5. 性能与体验观察

公开 PageSpeed Insights 接口在本次审计中返回 HTTP 429，因此本报告**没有伪造 Lighthouse 分数**。应在修复发布缺失模块后，通过 Search Console 和真实浏览器重新建立移动端基线。

当前静态观察：

- 首页 HTML：约 17.7 KiB。
- 首图：约 64 KiB。
- GSAP + ScrollTrigger：约 113.8 KiB。
- CSS：约 20 KiB。
- favicon：约 97.9 KiB，明显偏大。
- 全部介绍页图片约 325 KiB，非首屏图片已 lazy-load。
- 所有静态资源响应均为 `Cache-Control: public, max-age=0, must-revalidate`，缺少长期缓存。

建议：

1. 先修复缺失的 `landing-motion.js`。
2. 将 favicon 转成小尺寸 PNG/ICO，目标低于 20 KiB。
3. 对带内容哈希的 CSS、JS、图片设置一年 immutable 缓存。
4. 只在需要动画的页面加载 GSAP；保留 `prefers-reduced-motion` 降级。
5. 上线后监测 LCP、INP、CLS，不用单次实验室分数代替真实用户数据。

## 6. 域名与品牌信任

`pages.dev` 可以被索引，但不适合作为长期品牌资产。新站本身外链少，使用平台子域名也不利于建立稳定品牌识别。

建议：

- 购买并绑定独立域名，例如 `photoatelier.app` 或可用的品牌域名。
- 所有 canonical、OG、sitemap、结构化数据统一使用正式域名。
- 从 pages.dev 到正式域名做 301，避免长期并行。
- 增加 About、Privacy、Terms、Contact 页面，说明本地优先、图库权限和内容版权边界。

## 7. 两周执行顺序

### 第 1–2 天：修复可抓取基础

- 修复 `landing-motion.js` 发布。
- 工作台改为 `noindex,follow`，修正 canonical/OG。
- 修复结构化数据类型和 Offer。
- 完成 Search Console、Bing 和分析接入。
- 更新 sitemap 的 lastmod，并提交。

### 第 3–7 天：建立内容入口

- 发布 3 个中文专题：参考图转方案、人像分镜、动作口令。
- 每篇加入产品截图、可下载模板和清晰内部链接。
- 生成独立 OG 图，不直接使用 1.6:1 的首屏图作为所有分享卡片。

### 第 8–14 天：完成海外入口

- 完整翻译英文首页，不保留中文主体段落。
- 发布 2 个英文专题，并进行母语校对。
- 准备 Reddit/Product Hunt 所需截图、短 GIF、隐私说明和反馈入口。
- 等 Search Console 已出现抓取和首批查询数据后再扩大传播。

## 8. 验收指标

发布后 14 天内至少满足：

- Search Console 已验证，sitemap 状态为成功。
- 首页和 6 个专题页均可 URL Inspection，canonical 与预期一致。
- `site:` 查询开始出现有效页面；以 Search Console 覆盖报告作为最终依据。
- 线上不存在 JavaScript MIME/404 错误。
- 英文页面主要可见内容全部为英文。
- 工作台不进入索引，公开内容页正常进入索引。
- 移动端真实用户 Core Web Vitals 开始产生数据。
- 可以看到“曝光 → 首页访问 → 登录/体验 → 首个方案”的基础漏斗。

## 9. 参考标准

- Google canonical 指南：https://developers.google.com/search/docs/crawling-indexing/canonicalization
- Google sitemap 指南：https://developers.google.com/search/docs/crawling-indexing/sitemaps/build-sitemap
- Google 多语言站点指南：https://developers.google.com/search/docs/specialty/international/localized-versions
- Google SoftwareApplication 结构化数据：https://developers.google.com/search/docs/appearance/structured-data/software-app
- Google people-first 内容指南：https://developers.google.com/search/docs/fundamentals/creating-helpful-content
- Web Vitals：https://web.dev/articles/vitals

