# PhotoAtelier 拍摄资源二级工作区交接

## 1. 当前版本与问题

截图对应当前 R4 界面版本，代码基线为 `feat-r4-d`。底层仍是 V2.5 领域架构与 Schema v5，不能把界面版本 R4、产品版本 3.0 和 Schema v5 混为一谈。

场地库、模特库的数据与旧页面仍然存在，并未被删除。它们消失的直接原因是 R4 将一级入口合并为“设备与 LUT”后：

- `legacy/index.html` 的 `ensureEquipmentLutModeControls()` 给资源页添加了 `r4-equipment-only`；
- `src/r4-shell.css` 在该状态下隐藏了 `resource-venue`、`resource-model` 和旧资源标签栏；
- 页面只保留“设备、LUT、仿色预览”三个模式。

本轮只恢复并重组资源工作流，不修改 Schema v5、Catalog 实体含义或方案状态机。

## 2. 最终信息架构

一级侧栏继续保持六项，避免重新变得拥挤：

1. 方案库
2. 新建方案
3. 参考图库
4. 拍摄资源
5. 拍摄日程
6. 设置

将当前“设备与 LUT”改为“拍摄资源”。进入后显示二级导航：

1. **本方案**：当前方案已选场地、人员、设备和 LUT 的汇总。
2. **场地**：全局场地库、场地详情和项目选择状态。
3. **人员**：模特资源、授权状态、档期和项目选择状态。
4. **设备**：自有、租赁、借用和愿望清单设备；显示可用性和数量。
5. **LUT**：现有 LUT 库与仿色预览，不改变 LUT 领域归属。

二级导航使用紧凑分段控件或页面标签，不再把四个库同时铺满一页。

## 3. 跳转与返回规则

| 来源 | 目标二级页 | 完成选择后的行为 |
|---|---|---|
| 新建方案 / 场景地点 | 拍摄资源 / 场地 | 回到新建方案并显示所选场地名称 |
| 新建方案 / 拍摄对象 | 拍摄资源 / 人员 | 回到新建方案并显示所选模特 |
| 正式方案 / 设备清单 | 拍摄资源 / 设备 | 回到方案详情并更新设备清单 |
| 正式方案 / 调色方向 | 拍摄资源 / LUT | 回到方案详情并更新 LUT |
| 侧栏“拍摄资源” | 拍摄资源 / 本方案 | 留在资源汇总页 |

统一入口建议为：

```text
openResourceWorkspace({
  section: "venue | talent | equipment | lut | summary",
  mode: "browse | select",
  projectId,
  planId,
  returnTo: "brief | plan-detail | resource-summary"
})
```

选择模式必须保留来源位置。用户取消时原表单内容不能丢失；选择完成后返回来源页面，而不是停在资源库。

## 4. 方案生成流程

保留当前四个快速模板与简短 Brief，不把设备、LUT 等全部塞回第一步。

1. 快速模板或简短 Brief 生成预选方案。
2. 场地和拍摄对象可手工填写，也可进入资源库选择。
3. 用户采用预选方案后，再补齐正式场地、人员、设备与 LUT。
4. 确认资源后再进入日程和现场执行。

设备选择不应成为生成预选方案的强制条件。无设备时可以生成推荐，但必须标记“建议租赁/待准备”，不能伪装为用户已拥有。

## 5. 数据和 Use Case 约束

- 查询统一使用 `context.v5.queries.resourceCatalog.get(projectId)`。
- 新增设备使用 `context.v5.catalog.addEquipmentItem()`。
- 新增场地使用 `context.v5.catalog.saveVenue()`。
- 新增模特使用 `context.v5.catalog.saveTalentProfile()`。
- 选入项目使用 `context.v5.catalog.assignResourceToProject()`。
- 移出项目只使用 `context.v5.catalog.removeResourceAssignment()`。
- 移出项目不得删除全局设备、场地或模特资源。
- LUT 继续使用现有 LUT/Post 能力，不改造成 `ResourceAssignment`。
- 模特必须显示授权状态；未授权不能在界面中伪装为可直接执行。
- 页面不得重新直接写旧 `pw_venues`、`pw_models` 或设备集合。

## 6. 页面结构

桌面端采用主从结构：

- 左侧：搜索、筛选、资源列表。
- 右侧：资源详情、可用性、项目关系和选择按钮。
- 顶部：二级导航、当前方案名称、已选数量。

移动端采用两层页面：

- 第一层只显示资源名称、缩略信息和选择状态。
- 点击后进入全屏详情页。
- 底部固定“用于当前方案”或“取消选择”按钮。

设备页至少支持：

- 分类：机身、镜头、灯光、稳定、收音、附件；
- 来源：自有、租赁、借用、愿望清单；
- 状态：可用、不可用、待充电、待检修；
- 数量、必需/可选；
- 选中后显示在“本方案”汇总和拍摄清单中。

## 7. 五个 Agent 工单

### P1 二级导航与选择上下文

负责一级入口改名、二级导航、`openResourceWorkspace()`、返回来源和空状态。只改壳层和路由，不实现各资源列表，不修改 V5。

验收：五个二级入口可独立打开；刷新不产生 `/legacy/legacy/`；取消选择可返回原页面。

### P2 场地库

负责场地列表、搜索筛选、详情、添加场地、选入/移出项目和从 Brief 回填。使用 `resourceCatalog`、`saveVenue()`、`assignResourceToProject()`。

验收：同一场地可被多个项目使用；从一个项目移出后全局场地及其他项目关系保留。

### P3 人员与模特库

负责模特列表、详情、授权状态、边界、档期说明、添加模特和项目选择。使用 `saveTalentProfile()` 与资源关系 Use Case。

验收：未授权状态清晰；选入项目不等于创建虚假授权；移出项目不删除模特档案。

### P4 设备与 LUT

负责设备列表、设备型号搜索、来源、数量、可用状态、项目选择；保留现有 LUT 和仿色预览并接入新的二级导航。

验收：设备可以多选；选择数量和必需状态可恢复；LUT 不被错误写入资源关系。

### P5 方案入口集成与 QA

在 P1-P4 合并后执行。负责新建方案的“从场地库选择/从模特库选择”、正式方案的设备/LUT入口、返回回填、移动端和回归测试。

验收：

- 快速模板仍能生成预选方案；
- 场地、模特、设备、LUT 都能从对应入口选择；
- 页面刷新后项目关系保留；
- 移除项目关系不删除全局资源；
- 390px 与桌面端无横向溢出；
- `npm run test:scope -- catalog`、`npm run test:scope -- planning`、相关浏览器测试通过。

## 8. 合并顺序

1. P1 先确定二级导航和选择上下文合同。
2. P2、P3、P4 基于 P1 并行开发，各自只写独立模块和测试。
3. 合并 P2、P3、P4。
4. P5 最后接入新建方案和正式方案，统一执行回归。

任何 Agent 如果需要改变 Schema v5、ResourceAssignment、Venue、TalentProfile、EquipmentItem 或 LUT 的既有含义，立即停止并报告：

```text
ARCHITECTURE DECISION REQUIRED
```
