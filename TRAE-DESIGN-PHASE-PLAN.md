# PhotoAtelier 下一阶段：产品体验与视觉系统计划

## 决策

当前 P0-P4 功能包尚在实施，**此阶段不改产品代码**。功能汇总并通过 P5 QA 后，再开展视觉与移动端实施。

下一阶段的目标不是“再加一个漂亮皮肤”，而是把 PhotoAtelier 从技术工作台变成摄影师能快速使用的作品前、中、后流程工具：少输入、先看到分镜、现场能执行、回家能复盘。

## 是否需要 Figma

需要。Figma 不替代代码，而是作为下一轮 UI 的单一视觉合同：

- 先做桌面 `1440px` 和手机 `390px` 两个关键画板；
- 用可点击原型验证“新建方案 -> 分镜 -> 日程 -> 现场执行”；
- 在 Figma 固定颜色、排版、间距、图标、卡片、抽屉、空状态和交互状态；
- Trae 只按已确认画板实现，不再由多个 agent 各自决定样式。

不建议在这一步立刻重做所有页面。先完成核心工作流的原型和组件规则，再逐页落地。

## 第一优先级：核心工作流重构

### 1. 方案不是表单，而是一个逐步收敛的拍摄任务

**目标流程：**

`选择/新建方案 -> 填写必要条件 -> 生成候选分镜 -> 编辑分镜 -> 确认 -> 加入日程 -> 现场执行 -> 复盘`

方案生成只强制收集：主题、人物/人数、场景或场地、用途、时间。其他信息放在“补充条件”中，并提供可解释的默认预设；这样新用户可以直接生成，专业摄影师也可以细化。

生成结果第一屏只显示：方案标题、拍摄条件摘要、镜头数量、总时长、设备/LUT 概览和完整分镜。参考、创意方向、造型、报价、交付节奏全部收在后续层级，按需要展开。

### 2. 分镜是核心对象

每张分镜卡只显示必须现场执行的信息：缩略参考图、景别、机位、动作口令、镜头/焦段、光线、预计时长、设备、LUT。点击才进入抽屉查看完整理由、替代镜头、姿势变化和参考关系。

编辑分镜优先采用抽屉或全屏详情，避免把所有字段铺满页面。分镜操作应固定为：替换参考、调整顺序、添加、复制、删除、标记完成。

### 3. 方案库成为真正的入口

方案库置于方案生成之前，分为：草稿、待拍摄、进行中、待交付、已完成。每个条目只有一个主操作，例如“继续完善”“进入现场模式”“安排拍摄”或“填写复盘”。

## 第二优先级：移动端不是缩小桌面端

### 手机端定位

手机端主要服务于三件事：快速建方案、找参考、拍摄现场执行；复杂资料管理和批量编辑仍保留给桌面端。

### 手机端信息架构

底部导航固定为：`方案`、`参考`、`日程`、`我的`。不保留桌面侧边栏。

- **方案：** 当前方案/分镜清单，创建入口采用分步引导，不显示长表单。
- **参考：** 大图优先的图库，支持主题、场景、光线、色调、横竖构图筛选；上传与选择同一入口。
- **日程：** 按日程卡查看，进入后直接显示当天分镜和准备清单。
- **我的：** 设备、LUT、模板、导出、设置、反馈，而非技术代理状态。

### 现场模式

这是最值得新增的移动端能力：进入已确认日程后，显示全屏分镜执行清单、当前镜头、参考图、动作口令、设备/LUT、计时、完成/补拍/备注。支持离线浏览已经选入该方案的内容，不要求摄影师理解本地代理或知识库连接。

## 第三优先级：视觉系统

### 视觉方向

定位为专业摄影工作台，不应像 AI 仪表盘或技术后台。建议采用克制的暖灰/炭黑基础、一个清晰强调色、真实摄影缩略图作为信息锚点。避免多种高饱和颜色、无意义渐变、层层卡片和长段说明文字。

### 需要在 Figma 固定的设计令牌

- 字号层级、行高、中文与英文混排规则；
- 8px 栅格、页面边距、桌面与手机断点；
- 主要按钮、图标按钮、分段控件、筛选器、标签、状态、表单、抽屉、弹窗；
- 分镜卡、参考图卡、日程条目、方案条目、空状态、加载和错误状态；
- 无障碍状态：键盘焦点、最小触控区、对比度、移动端文字不溢出。

### 必须解决的体验问题

1. 不再直接把所有功能、字段和说明铺在同一屏。
2. 没有个人图库或本地服务时，产品仍然自然可用，技术连接信息只出现在设置中。
3. 每个按钮要么可用，要么明确不可用原因；不保留“点了没反应”的演示控制。
4. 当前选中的设备、LUT、参考图必须在分镜、方案和日程中一致可见。
5. 报价、批量工具和导出放在计划的末端，不干扰构思与分镜。

## 第四优先级：功能补强顺序

在当前 P0-P4 完成后，建议按此顺序继续：

1. **移动端现场模式：** 分镜勾选、补拍、现场备注、离线已选素材。
2. **完整方案导出：** 可打印/分享的网页与 PDF，包含分镜、参考、设备、LUT、日程和交付节奏。
3. **模板与预设：** 让摄影师保存自己的方案模板、设备套装、LUT 套装和报价规则。
4. **复盘回流：** 把完成镜头、废片原因、客户反馈、最终 LUT 回写到方案和个人知识库。
5. **新用户引导与反馈：** 示例方案、公开图库、反馈入口、版本反馈分类。先收集真实使用反馈，再决定 AI 视觉分析或外部图库深度接入。

不建议当前优先做：真实外部账户深度接入、自动抓取收藏、复杂地图/天气、自动风格迁移、多角色协作。这些都依赖稳定的方案与素材闭环，过早接入只会增加不稳定性。

## 建议的执行节奏

### 阶段 D0：功能汇总

前提：P0-P4 完成，P5 QA 通过。此时只修功能回归，不做视觉重构。

### 阶段 D1：UX 审计与 Figma 原型

产物：现有桌面流程审计、桌面/手机信息架构、两个核心原型（方案生成与现场模式）、设计令牌和组件清单。

验收：用户能在原型中完成一次“主题 -> 分镜 -> 日程 -> 现场执行”，不需要理解系统内部术语。

### 阶段 D2：桌面端实现

优先实现方案库、方案生成、分镜详情、参考图库、日程，按页面分包给 Trae。每页应有 1440px 与 390px 截图验收。

### 阶段 D3：移动端实现

先做方案、参考、日程和现场模式。移动端不应只是媒体查询后的压缩布局，而是使用独立的导航和分步交互。

### 阶段 D4：试用与迭代

开放小范围摄影师试用，设置可选反馈入口。每周只处理高频阻塞问题，避免反馈驱动的随机加功能。

## 下一次交给 Codex 的输入

1. P5 QA 的完整报告和截图；
2. 想保留的现有视觉截图，以及 2-3 个你喜欢的摄影/创作类产品参考；
3. 是否优先做“现场模式”还是“新用户公开试用与反馈”。

收到这些后，先用 Product Design 做审计和 3 个视觉方向，再由你选一个方向进入 Figma/实现，不直接改现有代码。
